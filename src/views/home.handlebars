<h1>mis productos</h1>

<div class="contenedorProductos">
  {{#each productos}}
    <div class="card">
      <p> {{this.title}} </p>
      <p> {{this.description}} </p>
      <p> {{this.price}} </p>
      <button>comprar</button>
    </div>
  {{/each}}
</div>